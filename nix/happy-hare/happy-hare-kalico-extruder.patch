diff --git a/extras/mmu_stepper.py b/extras/mmu_stepper.py
--- a/extras/mmu_stepper.py
+++ b/extras/mmu_stepper.py
@@ -676,7 +676,10 @@ class MmuStepper(ExtruderStepper):
 
         printer_extruder = self.printer.lookup_object(self.name, None)
         if isinstance(printer_extruder, PrinterExtruder):
-            self.stepper.set_position([printer_extruder.last_position, 0., 0.])
+            extruder_position = printer_extruder.last_position
+            if isinstance(extruder_position, (list, tuple)):
+                extruder_position = sum(extruder_position)
+            self.stepper.set_position([extruder_position, 0., 0.])
             self.stepper.set_trapq(printer_extruder.trapq)
         else:
             self.stepper.set_position([pos, 0., 0.])
@@ -696,10 +699,13 @@ class MmuStepper(ExtruderStepper):
 
         # Restore extruder kinematics allocated by stock ExtruderStepper
         self.stepper.set_stepper_kinematics(self.sk_extruder)
-        self.stepper.set_position([extruder.last_position, 0., 0.])
+        extruder_position = extruder.last_position
+        if isinstance(extruder_position, (list, tuple)):
+            extruder_position = sum(extruder_position)
+        self.stepper.set_position([extruder_position, 0., 0.])
         self.stepper.set_trapq(extruder.get_trapq())
 
-        self.commanded_pos = extruder.last_position
+        self.commanded_pos = extruder_position
         self.motion_mode = self.MODE_EXTRUDER
         self.motion_queue = extruder.get_name()
         self.manual_motion_queue = None
diff --git a/extras/mmu/unit/mmu_extruder_wrapper.py b/extras/mmu/unit/mmu_extruder_wrapper.py
--- a/extras/mmu/unit/mmu_extruder_wrapper.py
+++ b/extras/mmu/unit/mmu_extruder_wrapper.py
@@ -71,8 +71,16 @@ class MmuExtruderWrapper():
         # Register event handlers
         self.printer.register_event_handler('klippy:connect', self._handle_connect)
         self.printer.register_event_handler('klippy:ready', self._handle_ready)
 
 
+    @staticmethod
+    def _link_extruder_stepper(printer_extruder, extruder_stepper):
+        if hasattr(printer_extruder, 'link_extruder_stepper'):
+            printer_extruder.link_extruder_stepper(extruder_stepper)
+        else:
+            printer_extruder.extruder_stepper = extruder_stepper
+
+
     def add_unit(self, mmu_unit):
         self.connected_units.append(mmu_unit)
 
@@ -94,12 +102,12 @@ class MmuExtruderWrapper():
         for key, value in self.old_ext_options.items():
             self.config.fileconfig.set(self.extruder_name(), key, value)
 
         # Now we can switch in homing MmuExtruderStepper
-        printer_extruder.extruder_stepper = self.homing_extruder_stepper
+        self._link_extruder_stepper(printer_extruder, self.homing_extruder_stepper)
         self.homing_extruder_stepper.stepper.set_trapq(printer_extruder.get_trapq())
         self.mmu.log_debug(f"Extruder {self.extruder_name()} replaced with homing extruder")
 
-        self._extruder_stepper = printer_extruder.extruder_stepper
+        self._extruder_stepper = self.homing_extruder_stepper
 
 
         # Find TMC for extruder for current control ---------------------------------------------------------
@@ -203,6 +211,8 @@ class MmuExtruderStepper(MmuStepper):
 
     # Override simply to add QUIET option to control console logging
     def cmd_SET_PRESSURE_ADVANCE(self, gcmd):
+        if hasattr(self, 'pa_model'):
+            return super(MmuExtruderStepper, self).cmd_SET_PRESSURE_ADVANCE(gcmd)
         pressure_advance = gcmd.get_float('ADVANCE', self.pressure_advance, minval=0.)
         smooth_time = gcmd.get_float('SMOOTH_TIME', self.pressure_advance_smooth_time, minval=0., maxval=.200)
         self._set_pressure_advance(pressure_advance, smooth_time)
diff --git a/extras/mmu/mmu_filament_movement.py b/extras/mmu/mmu_filament_movement.py
--- a/extras/mmu/mmu_filament_movement.py
+++ b/extras/mmu/mmu_filament_movement.py
@@ -1692,7 +1692,7 @@ class MmuFilamentMovement:
 
         # Lock the extruder stepper
         stepper_enable = self.printer.lookup_object('stepper_enable')
-        extruder_stepper = self.toolhead.get_extruder().extruder_stepper.stepper
+        extruder_stepper = u.extruder_stepper_obj().stepper
         ge = stepper_enable.lookup_enable(extruder_stepper.get_name())
         ge.motor_enable(self.toolhead.get_last_move_time())
 
@@ -2921,7 +2921,7 @@ class MmuFilamentMovement:
             self.p.extruder_form_tip_current,
             "for tip forming move",
         ):
-            extruder_stepper = self.toolhead.get_extruder().extruder_stepper.stepper
+            extruder_stepper = u.extruder_stepper_obj().stepper
             initial_mcu_pos = extruder_stepper.get_mcu_position()
             initial_encoder_position = self.get_encoder_distance()
 
